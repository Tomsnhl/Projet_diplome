{% extends 'back/base.html.twig' %}

{% block title %}Message{% endblock %}

{% block subBody %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ message.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_message_list') }}">back to list</a>

    <a href="{{ path('app_back_message_edit', {'id': message.id}) }}">edit</a>

    {{ include('back/message/_delete_form.html.twig') }}
{% endblock %}
