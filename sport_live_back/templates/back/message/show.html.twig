{% extends 'back/base.html.twig' %}

{% block title %}Message{% endblock %}

{% block subBody %}
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ messages.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ messages.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_message_index') }}">back to list</a>

    <a href="{{ path('admin_message_edit', {'id': messages.id}) }}">edit</a>

    {{ include('back/message/_delete.html.twig') }}

    <a href="{{ path('admin') }}">retour au DashBoard</a>    
{% endblock %}
